DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
# Vercel Blob store ID (public store). Authenticates via VERCEL_OIDC_TOKEN,
# which `vercel env pull` provides automatically when linked to the project.
BLOB_STORE_ID_PUBLIC_STORE_ID="store_..."
